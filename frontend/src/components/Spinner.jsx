import { Puff } from "react-loader-spinner";

const Spinner = () => {
  return (
    <div className="Spinner">
      <Puff color="violet" height={80} width={80} />
    </div>
  );
};

export default Spinner;
